version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
  pre_build:
    commands:
      - pip install -r requirements.txt -t build/
  build:
    commands:
      - cd build/
      - zip -r lambda_function.zip .
      - aws s3 cp lambda_function.zip s3://dafdfedfad/
